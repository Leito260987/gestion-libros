package com.universidad.biblioteca.entity.enums;

/**
 * Roles del sistema. El prefijo ROLE_ es requerido por Spring Security
 * cuando se usa hasRole()/hasAnyRole().
 */
public enum RolNombre {
    ROLE_ADMIN,
    ROLE_USER
}
